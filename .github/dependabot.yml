# Please see the documentation for all configuration options:
# https://docs.github.com/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file

version: 2
updates:
  - package-ecosystem: "gomod"
    directory: "/"
    # Go modules have no lockfile; preserve public minimums while retaining
    # Dependabot security updates.
    open-pull-requests-limit: 0
    schedule:
      interval: "quarterly"
      time: "08:00"
      timezone: "Europe/Helsinki"
    cooldown:
      default-days: 1
    commit-message:
      prefix: "chore(deps)"
    labels:
      - "dependencies"
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "quarterly"
      time: "09:00"
      timezone: "Europe/Helsinki"
    cooldown:
      default-days: 1
    commit-message:
      prefix: "chore(deps)"
    labels:
      - "dependencies"
    groups:
      updates:
        patterns:
          - "*"
        update-types:
          - "patch"
          - "minor"
      major-updates:
        patterns:
          - "*"
        update-types:
          - "major"
